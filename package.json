{
  "name": "dcs-assessment-command-center",
  "version": "2.0.0",
  "private": true,
  "description": "Multi-user Data-Centric Security assessment, evidence, findings, and reporting platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node --watch server.js",
    "test": "node --test test/*.test.js",
    "check": "node --check server.js && node --check src/app.js && node --check src/db.js && node --check src/security.js && node --check src/rbac.js && node --check src/audit.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^8.0.1",
    "helmet": "^8.1.0",
    "pg": "^8.16.3"
  }
}
